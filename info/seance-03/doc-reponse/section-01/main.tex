\section{Création d'une pile}
\begin{enumerate}
  \item \q{Créer un module appelé }\il{Pile}\q{ qui contient les fonctions suivantes :}
        \begin{itemize}
          \item \il{creer_pile} \q{qui crée une pile vide sous la forme d'une liste vide}
          \item \il{empiler} \q{qui prend en argument une pile }\il{p}\q{ et un élément }\il{e}\q{ et l'empile sur la pile}
          \item \il{sommet} \q{qui prend en argument une pile }\il{p}\q{ et renvoie son sommet sans modifier la pile}
          \item \il{depiler} \q{qui prend en argument une pile }\il{p}\q{ et dépile son sommet}
          \item \il{hauteur} \q{qui prend en argument une pile }\il{p}\q{ et renvoie le nombre d'éléments de cette pile}
          \item \il{pile_vide} \q{qui prend en argument une pile }\il{p}\q{ et renvoie }\il{True}\q{ si elle est vide, }\il{False}\q{ sinon}
        \end{itemize}
        Pour des raisons évidentes, je choisis de créer une classe \il{Pile} et de transformer toutes ces fonctions en méthodes.
        Pour plus de classe, je renomme ces méthodes en anglais.
        \codeFromFile{section-01/code.py}
\end{enumerate}
