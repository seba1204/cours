\section{Début}
\begin{enumerate}[(a)]
  \item \q{Ecrire cette procédure }\il{tri_debut(A, i)}\q{ en utilisant la
          variable locale }\il{V = A[i]}\q{ et la variable globale }\q{A}\q{.}\\

        Du coup pour moi la variable globale \il{A} sur le paramètre \il{A} de
        l'objet \il{Ellipse}.

        \codeFromFileT{ressources/liste.py}{section-01/qa.py}

\end{enumerate}
