\section{Zéro d'une fonction}
\begin{enumerate}
  \item \q{Ecrire une fonction }\il{zero_fonction(f, a, b, eps)}
        \q{ qui calcule le zéro d'une fonction réelle $f(x)$ sur l'intervalle $[a, b] \in\mathbb{R}$,
          avec une précision $eps$. La fonction $f$ ne s'annule qu'une seule et unique fois sur $[a, b]$.
          Pour touver ce zéro, procéder par dichotomie.}
        \codeFromFile{section-05/code.py}
\end{enumerate}
