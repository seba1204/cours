\section{Liste de mots}
\begin{enumerate}
  \item \q{Dans l'aide }
        \inlineCode{help(str)}
        \q{ trouver la fonction qui renvoie vrai si l'argument transmis est une majuscule, faux sinon.}
        \begin{result}
          La fonction est \inlineCode{isUpper}.
        \end{result}
  \item \q{Ecrire une fonction qui convertit une phrase en la liste de ses mots (sans utiliser }\il{split}\q{).}
        \codeFromFile{section-04/q2.py}
  \item \q{Ecrire la fonction }\il{motmaj}\q{ qui prend en argument un texte et en extrait tous les mots qui commencent par une majuscule. La fonction renverra une liste de ces mots.}
        \codeFromFile{section-04/q3.py}
\end{enumerate}
