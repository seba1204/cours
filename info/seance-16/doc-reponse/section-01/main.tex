\section{Préparation des fonctions élémentaires}
On pose $f(x, y) = x^2 + 2y^2 + 2xy$
\begin{enumerate}[(a)]
  \item \q{Enregistrer la fonction }\il{f(u)}\q{qui prend pour seule entrée la
          liste ou tableau }\il{u = [x, y]}\q{ et retourne $f(x, y)$.}
        J'importe d'abord les modules :
        \codeFromFileT{main.py}{section-01/q0.py}
        Puis je code $f$ :
        \codeFromFileT{main.py}{section-01/qa.py}
  \item \q{Enregistrer }\il{Nabla(u)}\q{ qui prend pour seule entrée la liste
          ou tableau }\il{u = [x, y]}\q{ et retourne le vecteur
          $\nabla f (x, y)$ sous forme de tableau de type }\il{array}\q{ de}
        \il{numpy}\q{.}

        \codeFromFileT{main.py}{section-01/qb.py}

  \item \q{Enregistrer }\il{Vunit(u)}\q{ qui prend pour seule entrée la liste
          ou tableau }\il{u = [x, y]}\q{ eet retourne le vecteur unitaire
          $\vec{\omega} = \frac{\vec{u}}{||\vec{u}||}$ sous forme de tableau de
          type }\il{array}\q{ de}\il{numpy}\q{.}

        \codeFromFileT{main.py}{section-01/qc.py}
\end{enumerate}
