\section{Mise au carré}
\begin{enumerate}
  \item \q{Ecrire une fonction }
        \inlineCode{liste_au_carre}
        \q{ qui prend en entrée une liste}
        \inlineCode{L}
        \q{ d'entiers et qui renvoie une liste de même taille dont chaque élément est le carré de même indice dans la liste d'entrée.}
        \codeFromFile{section-03/code.py}
\end{enumerate}
