\q{Ecrire une fonction }\il{pascal(n)}\q{ qui donne le triangle de Pascal avec
$n$ lignes $n$ colonnes donc contenant
$\left(
  \begin{array}{c}
      i \\j
    \end{array}
  \right)$ pour les lignes $i\in [\![0, n-1]\!]$ et les colonnes $j\in
  [\![0, n-1]\!]$.}
\codeFromFileT{exercice.py}{section-02/q3/1.py}
