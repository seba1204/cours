\q{Ecrire une fonction }\il{partition(A, g, d)}\q{ qui renvoie la nouvelle place
  qu'aurait l'élément }\il{V = A[g]}\q{ appelée pivot dans la partie }\il{A[g:d]}
\q{si celle-ci était triée sans tenir compte des autres parties de la liste.}

\codeFromFileT{main.py}{section-01/q2/1.py}
