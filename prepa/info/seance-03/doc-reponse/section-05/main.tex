\section{Création d'une file}
\begin{enumerate}
  \item \q{Créer un module appelé }\il{File}\q{ qui contient les fonctions suivantes :}
        \begin{itemize}
          \item \il{creer_file} \q{qui crée une file vide sous la forme d'une liste vide}
          \item \il{push} \q{qui prend en argument une file }\il{f}\q{ et un élément }\il{e}\q{ et l'insère dans la file}
          \item \il{pop} \q{qui prend en argument une file }\il{f}\q{ et enlève et renvoie l'élément en tête de file}
          \item \il{longueur} \q{qui prend en argument une file }\il{f}\q{ et renvoie le nombre d'éléments de cette file}
          \item \il{file_vide} \q{qui prend en argument une file }\il{f}\q{ et renvoie }\il{True}\q{ si elle est vide, }\il{False}\q{ sinon}
        \end{itemize}
        De même, je choisis de créer une classe \il{File}, de transformer toutes ces fonctions en méthodes et je renomme ces méthodes en anglais.
        \codeFromFile{section-05/code.py}
\end{enumerate}
